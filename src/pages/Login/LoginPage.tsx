import { Login } from '../../components'
import './LoginPage.css'

export const LoginPage: React.FC = () => {

    return (
        <div className="login">
            <Login />
        </div>
    )
}